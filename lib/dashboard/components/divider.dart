import 'package:flutter/material.dart';

Widget divider() {
  return Padding(
      padding:
          EdgeInsets.only(top: 10.0, bottom: 10.0, left: 30.0, right: 30.0),
      child: Divider(
        indent: 10.0,
      ));
}
